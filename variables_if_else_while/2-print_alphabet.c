#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * Return: 0 (success)
 */
int main(void)
{
	int yass;

	for (yass = 'a'; yass <= 'z'; yass++)
		putchar(yass);
	putchar('\n');

	return (0);
}
